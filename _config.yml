title: VeriBuild
description: "Escaping Dependency Hell: Finding Build Dependency Errors with the Unified Dependency Graph"
author:
  name: "veribuild"
  email: "veribuild.icse@gmail.com"
  url: ""

baseurl: ""

source: .
destination: ./_site
permalink: /:title
paginate: 3
paginate_path: /page/:num/

defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

future: true
plugins:
  - jekyll-seo-tag
  - jekyll-paginate
  - jekyll-sitemap

# Custom variables
owner : "veribuild"
year : "2019"
